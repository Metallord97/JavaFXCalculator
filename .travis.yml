language: java 
jdk:
- openjdk8
addons:
  sonarcloud:
    organization: "metallord97"
    token:
      secure: "travis encrypt d34324b07f0b12b34dcd6b206f1abba3fc74b586" # encrypted value of your token

script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - sonar-scanner
  - ant build